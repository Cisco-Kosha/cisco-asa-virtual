{
  "version": "2.0.0",
  "fields": [
    {
      "field": "USERNAME",
      "displayName": "Cisco ASAv Username",
      "type": "String",
      "minLength": 4,
      "maxLength": 60,
      "required": true,
      "description": "Cisco ASAv Host username",
      "example": "admin",
      "helpSection": "Cisco ASAv host username"
    },
    {
      "field": "PASSWORD",
      "displayName": "Cisco ASAv Password",
      "type": "password",
      "minLength": 4,
      "maxLength": 60,
      "required": true,
      "description": "Cisco ASAv Host password",
      "example": "admin",
      "helpSection": "Cisco ASAv Host password"
    },
    {
      "field": "SECURE",
      "displayName": "Secure Connection",
      "type": "String",
      "minLength": 4,
      "maxLength": 10,
      "required": true,
      "description": "Cisco ASAv Secure Connection",
      "example": "false",
      "helpSection": "If Cisco ASAv Host connection is secured by HTTPS/TLS, enter true. Otherwise if it is backed by self-signed certificate or not backed by HTTPS at all, then enter false"
    },
    {
      "field": "SERVER_URL",
      "displayName": "Cisco ASAv Host URL",
      "type": "String",
      "minLength": 4,
      "maxLength": 60,
      "required": true,
      "description": "Cisco ASAv Host URL",
      "example": "http://10.10.10.10",
      "helpSection": "Cisco ASAv Host URL"
    }
  ]
}